altair==6.2.2
anyio==4.14.2
attrs==26.1.0
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
et_xmlfile==2.0.0
fonttools==4.63.0
h11==0.16.0
httptools==0.8.0
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kiwisolver==1.5.0
MarkupSafe==3.0.3
matplotlib==3.11.0
narwhals==2.24.0
numpy==2.5.0
openpyxl==3.1.5
packaging==26.2
pandas==3.0.3
pillow==12.2.0
protobuf==7.35.1
pyarrow==24.0.0
pydeck==0.9.3
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
six==1.17.0
starlette==1.3.1
streamlit==1.61.1
tenacity==9.1.4
toml==0.10.2
typing_extensions==4.16.0
tzdata==2026.2
urllib3==2.7.0
uvicorn==0.52.1
watchdog==6.0.0
websockets==16.1.1

# ---------------------------------------------------------------------------
# Capa de IA del asistente (opcional)
# ---------------------------------------------------------------------------
# Sin esto el asistente funciona igual en modo local: buscador de
# documentacion, glosario y explicador de la corrida. Solo hace falta para el
# chat y el agente del sandbox.
#
# OJO: es `google-genai`, NO `google-generativeai`. La segunda es la SDK vieja,
# esta archivada y no soporta los modelos nuevos. Si alguna vez quedo instalada,
# desinstalarla: las dos comparten el namespace `google` y conviven mal.
#
# Va sin pin porque no se cual es la ultima version estable al momento de
# escribir esto. Cuando el deploy funcione, fijarla como el resto del archivo:
#     pip freeze | Select-String google-genai      (PowerShell)
#     pip freeze | grep google-genai               (bash)
google-genai

# Si en vez de Gemini se usa Anthropic, comentar la linea de arriba y
# descomentar esta. No hace falta tener las dos: el asistente elige el
# proveedor segun que key este cargada (ver docs/asistente.md).
# anthropic

# ---------------------------------------------------------------------------
# Perfilador (opcional, chico)
# ---------------------------------------------------------------------------
# `ui/perfil.py` lo usa para mostrar la memoria del proceso. Sin psutil el
# panel muestra los tiempos por tab pero no la memoria, que en Streamlit
# Community Cloud —1 GB, y reinicia la app al pasarse— suele ser el dato que
# explica los sintomas que los tiempos no explican.
# Se enciende con el secreto `PERFIL = true`; apagado no cuesta nada.
psutil
